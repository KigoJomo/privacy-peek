# Can never be too ambitious, right?

- Add stale analysis check - or just figure out cron jobs for automated re-analysis. That way it's not triggered by user query, hence a faster response - better UX.

- Tighten up the scoring rubric a bit - sites are performing a bit too well.

- Add list view for recent sites, perhaps even a full sortable table.
